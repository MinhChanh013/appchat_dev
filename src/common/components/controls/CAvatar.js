import React from "react";
import Avatar from "@mui/material/Avatar";
const CAvatar = ({ image }) => {
  return <Avatar alt="Remy Sharp" sx={{ width: 45, height: 45 }} src={image} />;
};

export default CAvatar;
